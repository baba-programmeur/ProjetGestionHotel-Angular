import { GlobalGenericValidator } from './global-generic-validator';

describe('GlobalGenericValidator', () => {
  it('should create an instance', () => {
    expect(new GlobalGenericValidator()).toBeTruthy();
  });
});
